<!DOCTYPE html>
<html>
<head>
    <title>FlexFlow - User Profile</title>
</head>
<body>
    {{> layouts/main}}
    
    {{> partials/header}}

    <div class="container">
        <h1>User Profile</h1>
        <div class="user-info">
            <p><strong>Username:</strong> {{user.username}}</p>
            <p><strong>Email:</strong> {{user.email}}</p>
        </div>
        <a href="/logout">Logout</a>
    </div>

    {{> partials/footer}}
</body>
</html>
